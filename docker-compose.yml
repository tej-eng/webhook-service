services:
  webhook-service:
    build: .
    restart: always
    environment:
      RABBITMQ_URL: amqp://guest:guest@dhwaniastro-rabbitmq:5672
      RAZORPAY_WEBHOOK_SECRET: nkdPt8OpyM5qj33oBqEPi4qo
      PORT: 8004
    ports:
      - "8004:8004"
    networks:
      - dhwaniastro-net  # use the external network

networks:
  dhwaniastro-net:
    external: true